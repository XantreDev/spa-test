import React from 'react'
import { cardData } from '../VideoWrapper'

const VideoLineWrapper: React.FC<cardData> = () => {
  return (
    <div>VideoLineWrapper</div>
  )
}

export default VideoLineWrapper